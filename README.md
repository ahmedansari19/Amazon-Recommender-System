# Amazon-Recommender-System
Collaborative filtering–based recommendation system trained on the Amazon 5-core dataset. Features matrix factorization models (SVD, ALS), sparse data handling, and ranking-based evaluation.
